---
"@lynx-js/rsbuild-plugin": patch
"@lynx-js/react-rsbuild-plugin": patch
"@lynx-js/rspeedy": patch
---

Default `performance.profile` to `true` in `pluginLynx` when `DEBUG` includes `lynx`, `rsbuild`, `rspeedy` or `*`, to align with Rspeedy.
