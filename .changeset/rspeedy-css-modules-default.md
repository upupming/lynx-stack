---
"@lynx-js/rspeedy": minor
---

Stop defaulting `output.cssModules.localIdentName` to `[local]-[hash:base64:6]`, so Rspeedy follows the Rsbuild default of `[path][name]__[local]-[hash:base64:6]` in development.
