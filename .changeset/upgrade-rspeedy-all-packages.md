---
"upgrade-rspeedy": patch
---

Upgrade every published `@lynx-js/` package, not just the ones a scaffolded project starts with. A project using an external bundle no longer has `@lynx-js/lynx-bundle-rslib-config` left behind while `@lynx-js/rspeedy` moves on.
