{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": [],
      "inputs": ["scripts/build.mjs"],
      "outputs": ["dist/**"]
    }
  }
}
