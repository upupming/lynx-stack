{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": [
        "@lynx-js/react-transform#build:wasm"
      ],
      "inputs": [
        "src"
      ],
      "outputs": ["dist/**"]
    }
  }
}
